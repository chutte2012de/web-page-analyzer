apiVersion: v1
kind: Service
metadata:
  name: web-page-analyzer-service
spec:
  selector:
    app: web-page-analyzer
  ports:
    - protocol: TCP
      port: 4010
      targetPort: 4010
  type: LoadBalancer
